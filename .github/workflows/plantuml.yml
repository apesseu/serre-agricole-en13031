name: PlantUML Diagram Generation & Deploy

on:
  push:
    branches:
      - main      # Déclenche quand tu pushes sur main

jobs:
  generate-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Récupération du repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Installer Java et Graphviz
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 11

      - name: Install Graphviz
        run: |
          sudo apt-get update
          sudo apt-get install -y graphviz

      # 3️⃣ Télécharger PlantUML
      - name: Download PlantUML
        run: |
          curl -L -o plantuml.jar "https://github.com/plantuml/plantuml/releases/latest/download/plantuml.jar"
          ls -lh plantuml.jar

      # 4️⃣ Vérifier les fichiers .puml
      - name: Check .puml files
        run: |
          echo "🔍 Recherche des fichiers .puml dans diagrams/"
          find diagrams -name "*.puml" || echo "⚠️ Aucun fichier .puml trouvé !"

      # 5️⃣ Génération des SVG en mode debug (fichier par fichier)
      - name: Generate PlantUML diagrams (debug mode)
        run: |
          mkdir -p docs
          echo "📊 Génération des diagrammes PlantUML vers docs/"
          for f in diagrams/*.puml; do
            echo "➡️ Génération de $f"
            java -jar plantuml.jar -tsvg -o ../docs "$f" || echo "❌ ERREUR sur $f"
          done
          
          echo "📂 Contenu de docs/ après génération :"
          ls -la docs/

      # 6️⃣ Créer une page index.html listant les SVG
      - name: Create index.html for GitHub Pages
        run: |
          echo "<html><head><title>PlantUML Diagrams</title></head><body><h1>Liste des diagrammes générés</h1><ul>" > docs/index.html
          for svg in docs/*.svg; do
            name=$(basename "$svg")
            echo "<li><a href=\"$name\">$name</a></li>" >> docs/index.html
          done
          echo "</ul></body></html>" >> docs/index.html

      # 7️⃣ Vérifier qu'il y a bien des SVG
      - name: Verify SVG generation
        run: |
          svg_count=$(find docs -name "*.svg" | wc -l)
          echo "📈 Nombre total de fichiers SVG : $svg_count"
          if [ "$svg_count" -eq 0 ]; then
            echo "❌ Aucun fichier SVG généré, on arrête."
            exit 1
          fi

      # 8️⃣ Déploiement automatique sur GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs
          publish_branch: gh-pages
